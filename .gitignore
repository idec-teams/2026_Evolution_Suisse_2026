# Build output
site/

# Local Python environment
.venv/
__pycache__/
*.py[cod]

# Editor / OS
.DS_Store
.idea/
.vscode/

# Structure-pipeline build env (dev only; the outputs it produces are
# committed under theme/data/, so a clone builds the site without it).
tools/.venv-build/
tools/structures/cache/
tools/sandbox/

tools/shots/
